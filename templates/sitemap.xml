<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
{% for entry in entries %}{% if not entry.extra.noindex | default(value=false) %}
  <url><loc>{{ entry.permalink }}</loc>{% if entry.updated %}<lastmod>{{ entry.updated }}</lastmod>{% endif %}</url>
{% endif %}{% endfor %}
</urlset>
